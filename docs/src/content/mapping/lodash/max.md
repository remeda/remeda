---
category: Math
remeda: firstBy
---

Use [`identity`](/docs#identity) as the criteria and `"desc"` as the direction.

```ts
// Lodash
max(DATA);

// Remeda
firstBy(DATA, [identity(), "desc"]);
```
