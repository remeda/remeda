---
category: Array
remeda: reverse
---

Lodash mutate's the input but in Remeda a new array is returned instead.

```ts
// Lodash
const DATA = [1, 2, 3];
reverse(DATA);

// Remeda
let DATA = [1, 2, 3];
DATA = reverse(DATA);
```
