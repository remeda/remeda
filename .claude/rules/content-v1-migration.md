---
paths:
  - "packages/docs/src/content/v1-migration/**/*.md"
---

# v1-migration Markdown Conventions

## Heading Levels

- Body sub-headings start at `####` (h4) — entries render inside the v1 function card's `<article>` whose function name is the `<h3>` (under an sr-only category `<h2>`)
