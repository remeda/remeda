---
paths:
  - "packages/docs/src/content/mapping/**/*.md"
---

# Mapping Doc Markdown Conventions

## Heading Levels

- Body sub-headings start at `####` (h4) — entries render inside an `<article>` whose function name is the `<h3>`
