# Skills

Agent-agnostic skill definitions used across the repo. For Claude Code:
`.claude/skills` is a symlink that points here.

## Local skills

Skills prefixed with `local-` (e.g. `local-my-skill`) are gitignored — they're
for per-contributor use and don't ship with the repo.
