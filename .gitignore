*.local.*
*.local

node_modules/

# LLMs sometimes run coverage testing manually in the root
coverage/

# We consider "local-" skills to be untracked private skills.
.agents/skills/local-*

.tmp/
.playwright-mcp/

.DS_Store

# Logo exploration scratch (preserved locally / in PR attachments, not in the repo)
.logo-scratch/
# Derived brand assets for manual upload (regenerate with `npm run assets -w @remeda/brand`)
packages/brand/dist/
