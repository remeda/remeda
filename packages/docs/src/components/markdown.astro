---
import { asHtml } from "@/lib/as-html";

type Props = {
  readonly markdown: string;
};

const { markdown } = Astro.props;
---

{/* eslint-disable-next-line astro/no-set-html-directive -- Intentional! */}
<Fragment set:html={asHtml(markdown)} />
